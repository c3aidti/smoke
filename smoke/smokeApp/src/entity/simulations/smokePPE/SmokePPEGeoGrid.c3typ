/**
Experimental typ to extract unique lat long pairs from the {@link GeoSurfaceTimePoint} type
*/
@db(index=["lat,long"])
entity type SmokePPEGeoGrid schema name "SPPE_GEO_GRID" {
    lat: !double
    long: !double
    geo: !GeoPoint
}