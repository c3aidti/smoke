/**
* Represents customization options for a {@link StressTestPGJob} batch job
*/
type StressTestPGJobOptions {
    // the number calls in each batch
    batchSize: int = 1
    // the number of batches
    nBatches: int = 1
}