/**
* Batch job to stress-test the {@link BatchQueue}
*/
type StressTestJob extends BatchJob<StressTestJob, StressTestJobOptions, StressTestJobBatch> type key 'STRSS_TST_JB' {
    doStart:        ~ js server
    processBatch:   ~ js server
    allComplete:    ~ js server
    failed:         ~ js server
}