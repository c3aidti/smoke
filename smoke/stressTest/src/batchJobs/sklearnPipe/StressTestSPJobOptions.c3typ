/**
* Represents customization options for a {@link StressTestSPJob} batch job
*/
type StressTestSPJobOptions {
    // the number calls in each batch
    batchSize: int = 1
    // the number of batches
    nBatches: int = 1
}