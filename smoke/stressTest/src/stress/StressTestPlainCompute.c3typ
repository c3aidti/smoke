/**
* Plain computation using basic Python libraries
*/

type StressTestPlainCompute {
    // run a single plain computation
    @py(env="stressPlainCompute_1_0_0")
    run: function(): string
    // send multiple computations to the {@link BatchQueue}
    runBatch: function(int nBatches, int batchSize): any js server
}